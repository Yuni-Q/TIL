#include <iostream>
#include <vector>
#include <queue>

using namespace std;

const int MAX = 1000 + 1;
int N, M;
int main(void)
{
  ios_base::sync_with_stdio(0);
  cin.tie(0);
  int N, T, P;
  cin >> N;
  int numArr[15][2] = {
      {0, 0},
  };
  cout << numArr[14][0];

  // for (int i = 0; i < N; i++)
  // {
  //   cin >> T >> P;
  // }

  return 0;
};